# U.S. Bank x NFL — Animated HTML Banners
**Handoff · July 28, 2026**

Paste the preview link where marked `[LINK]`.

---

## Send-off note

**Subject:** U.S. Bank x NFL — animated banner previews for review

Hi [name],

Here are the animated banners for review: **[LINK]**

Three creative directions, each built in three sizes — 300x600, 728x90 and 440x460. Everything on that page is the live HTML that will traffic, so what you see is exactly how the units will run. There's a **Replay all** button at the top to rewatch the animations.

A couple of notes:

- Layouts, copy and photography match the approved designs.
- The click-through URL is a placeholder for now — send the final landing pages and we'll swap them in.
- The remaining sizes will follow once the static set is finalized.

Any changes, send them over and we'll turn them around.

Thanks,
Sofia

---

## Deliverables

| | 300x600 | 728x90 | 440x460 |
|---|---|---|---|
| Your wins are our wins | ✓ | ✓ | ✓ |
| The smartest play? Banking together | ✓ | ✓ | ✓ |
| Experts always in your corner | ✓ | ✓ | ✓ |

9 animated HTML5 units. Delivered as unzipped folders and as individual zips for trafficking.

**Animation** — the frame opens white, then a single sweep carries the whole composition in at once (photo, navy panel, logos and type together), and the CTA opens last. Verticals reveal top-to-bottom; the wide and square sizes sweep in from the left.

**Build** — all type, logos and gradients are the Figma artwork itself, exported as vectors and placed at their original coordinates, so there is no font substitution. Photo crops reproduce the Figma image transforms.

**Specs** — GSAP 3.13 from the standard Google ad CDN, `clickTag` wired, `ad.size` meta set, static fallback frame if scripts are blocked. Heaviest zip 112KB, well under the 150KB limit.

## Outstanding

1. **Click-through URL(s)** — currently `https://www.usbank.com/`. One per design, or one for all?
2. **Remaining sizes** — 160x600, 300x250, 300x60, 320x50, 970x66, 970x90, 970x250, to be built off the final statics.

## Files

- `banners/` — 9 unzipped banner folders
- `zips/` — 9 zips for trafficking
- `preview.html` — local preview page
- Build source (generator, Figma exports, photo masters): `~/Desktop/USBank_NFL_Banners/build/`
